apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-server-ingress
spec:
  rules:
    - host: http://35.228.57.183.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: app
                port:
                  number: 8080